# Discord Webhook URL
# To create a webhook:
# 1. Open Discord and go to your server
# 2. Right-click on the channel where you want alerts -> Edit Channel
# 3. Go to Integrations -> Webhooks -> New Webhook
# 4. Give it a name (e.g., "Polymarket Whales") and copy the webhook URL
# 5. Paste the URL below
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Whale trade threshold in USD (trades >= this value trigger alerts)
WHALE_THRESHOLD_USD=10000

# How often to check for market resolutions (in hours)
RESOLUTION_CHECK_INTERVAL_HOURS=1

# SQLite database path (relative to project root)
DATABASE_PATH=polymarket_whales.db

# Data retention: delete resolved trades older than this many days
# Unresolved trades are always kept. Wallet stats are preserved.
DATA_RETENTION_DAYS=30

# =============================================================================
# Correlation Checker (optional)
# Detects whale trades that precede related news articles
# =============================================================================

# Discord webhook for correlation alerts (separate channel recommended)
CORRELATION_WEBHOOK_URL=https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN

# Path to news scraper database
# Local: /Users/yourname/projects/news-scraper/articles.db
# Droplet: /var/lib/news-scraper/articles.db
NEWS_DB_PATH=/var/lib/news-scraper/articles.db

# Path to scanner database (usually same as DATABASE_PATH)
# Local: /Users/yourname/projects/polymarket-whale-alerts/polymarket_whales.db
# Droplet: /var/lib/polymarket-scanner/polymarket_whales.db
SCANNER_DB_PATH=/var/lib/polymarket-scanner/polymarket_whales.db
